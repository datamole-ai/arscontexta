{
  "description": "Ars Contexta plugin hooks — session orientation",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-orient.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
